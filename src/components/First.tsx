import React from 'react';

const First = () => {
  return (
    <section className="h-screen flex items-center justify-center bg-blue-700 text-white text-4xl">
      First Section (Pinned)
    </section>
  );
};

export default First;
